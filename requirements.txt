# Pinned from the `bio` conda environment (Python 3.12.8).
# These versions are part of the reproduction contract: the metrics stored in the
# legacy project are only guaranteed reachable under them.
# Re-frozen from the live environment in Phase 3.

polars==1.17.1
pandas==2.2.3
numpy==1.26.0
pyarrow==18.1.0
scipy==1.13.1
statsmodels==0.14.4

scikit-learn==1.5.2
xgboost==2.1.3
lightgbm==4.5.0
catboost==1.2.7
bayesian-optimization==2.0.3

shap==0.45.0
fasttreeshap==0.1.6
gseapy==1.1.8

matplotlib==3.9.4
seaborn==0.13.2
openpyxl==3.1.5

jupyterlab==4.3.4
PyYAML==6.0.2
